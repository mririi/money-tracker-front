import { AbstractCustomModalDirective } from './abstract-custom-modal.directive';

describe('CustomModalDirective', () => {
  it('should create an instance', () => {
    const directive = new AbstractCustomModalDirective();
    expect(directive).toBeTruthy();
  });
});
